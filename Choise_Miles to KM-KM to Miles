def convert_km (miles):
    km = miles * 1.60934
    return km
zahl=float(input("Gib die Milen-Anzahl an, die du in KM umrechnen willst? :"))
distance = convert_km (zahl)

print("Ca.", float(distance), "KM.")
